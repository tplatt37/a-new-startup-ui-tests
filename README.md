# Overview

This is the Selenium python code used to perform a UI test on the A-New-Startup application.

It is meant to be used as a container image, based on Selenium's headless Chrome image.

(This way we can easily run it as part of your CI/CD Pipeline)

This is not a stand-alone demo.

